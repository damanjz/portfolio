import { DetailBar } from "daman-portfolio";

/** Case-study page top bar — identity + back link. */
export function Default() {
  return (
    <div style={{ minHeight: 120 }}>
      <DetailBar />
    </div>
  );
}
