import { Hero } from "daman-portfolio";
import { Settle } from "./_settle";

/** The full hero — boot line, display name, role, thesis, CTAs. */
export function Default() {
  return (
    <>
      <Settle />
      <Hero />
    </>
  );
}
