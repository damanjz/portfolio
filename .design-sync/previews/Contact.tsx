import { Contact } from "daman-portfolio";
import { Settle } from "./_settle";

/** Contact — prompt-styled CTA with email button and social links. */
export function Default() {
  return (
    <>
      <Settle />
      <Contact />
    </>
  );
}
