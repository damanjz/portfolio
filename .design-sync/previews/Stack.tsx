import { Stack } from "daman-portfolio";
import { Settle } from "./_settle";

/** The stack readout — tools grouped by domain. */
export function Default() {
  return (
    <>
      <Settle />
      <Stack />
    </>
  );
}
